{
  "name": "vedcompany-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "VED Capacitor Android shell (loads https://vedcompany.ru)",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "add:android": "npx cap add android",
    "build:apk": "cd android && .\\gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "typescript": "^5.7.2"
  }
}
